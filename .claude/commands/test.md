Run the test suite with verbose output and report results.

1. Run `uv run python -m pytest tests -v --tb=short`
2. If tests fail, analyze the failures
3. Report: total tests, passed, failed, and any error details
