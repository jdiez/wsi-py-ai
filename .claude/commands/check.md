Run the full quality suite: lock consistency, pre-commit, mypy, and deptry. Report results.

1. Run `make check` and capture output
2. If any step fails, identify the specific error and fix it
3. Re-run until all checks pass
4. Report summary of what passed/failed/was fixed
